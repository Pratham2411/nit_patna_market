# Local dev: leave unset — Vite proxies /api to http://localhost:5001
# Production (Vercel): set VITE_API_URL in the Vercel dashboard (not in this file)
# VITE_API_URL=https://your-backend.onrender.com/api
